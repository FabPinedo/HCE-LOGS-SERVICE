# ms-tch-audit-logger — Audit Logger Service
# Generated by Jarvis Platform
# Copiar a .env y completar los valores marcados con ***

# ── Servidor ──────────────────────────────────────────────
PORT=10400
NODE_ENV=development

# ── Seguridad — API Key HTTP ───────────────────────────────
# Protege los endpoints GET /audit/* con una clave interna.
# Si está vacío el guard es permisivo (útil en desarrollo).
# En producción SIEMPRE establecer un valor largo y aleatorio.
# Uso: incluir header  x-api-key: <valor>  en cada request.
AUDIT_API_KEY=

# ── Cifrado de payloads — AES-256-GCM (S5) ───────────────
# Clave de 32 bytes UTF-8 para cifrar payload_encrypted en la tabla audit_event.
# Si está vacío los payloads se guardan como JSON plano (solo en desarrollo).
# En producción SIEMPRE establecer un valor aleatorio de exactamente 32 caracteres.
# Ejemplo: openssl rand -base64 24 | tr -d '=' | head -c 32
AUDIT_PAYLOAD_KEY=

# ── Kafka ─────────────────────────────────────────────────
# Este servicio ES el consumer central de auditoría: siempre escucha Kafka,
# no tiene flag de activación/desactivación (esa flag es solo para los
# productores — los demás MS/gateways — que deciden si publican o no).
# Broker(s) separados por coma — este servicio actúa como CONSUMER
KAFKA_BROKER=localhost:9092
# Topic del que consume eventos de auditoría
KAFKA_TOPIC=platform.logs

# ── SQL Server ────────────────────────────────────────────
DB_HOST=
DB_PORT=1433
DB_USER=
DB_PASS=
DB_NAME=audit_db
# Instancia nombrada de SQL Server (dejar vacío si usa puerto directo)
DB_INSTANCE=
